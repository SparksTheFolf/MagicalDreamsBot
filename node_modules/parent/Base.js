'use strict';

var _ = require('lodash-node');
var inherit = require('./util/inherit');

/**
 * @class Base
 * @extends Object
 * */
var Base = inherit(Object, /** @lends Base.prototype */ {

}, /** @lends Base */ {

    /**
     * @public
     * @static
     * @memberOf Base
     * @method
     *
     * @param {Object} [members]
     * @param {Object} [statics]
     *
     * @returns {Function}
     * */
    extend: function extend (members, statics) {
        statics = _.extend({extend: extend}, statics);

        return inherit(this, members, statics);
    }
});

module.exports = Base;
